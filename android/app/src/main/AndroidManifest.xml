<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Not used for sensor reads: Android 14+ requires one of BODY_SENSORS /
         HIGH_SAMPLING_RATE_SENSORS / ACTIVITY_RECOGNITION before a service may
         start with foregroundServiceType="health" (WaterTrackingService).
         Removing it makes that service throw on Android 14+. -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <!-- The AI chat calls server_gateway_ai. Firebase's own manifests happen to
         merge this in too, but the app's own network call should not depend on
         a plugin declaring it. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Health Connect: the app writes each logged drink as a hydration record.
         Write only — it never imports other apps' hydration data, and deleting
         its own records needs no read access. Opt-in: nothing is written until
         the user turns the Settings toggle on and grants the permission.
         This must stay in step with the published privacy policy, which
         declares write-only hydration access. -->
    <uses-permission android:name="android.permission.health.WRITE_HYDRATION" />

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect on Android 13 and below asks the app to explain
                 why it wants the permissions; it opens this activity. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Android 14+ routes the same rationale through a dedicated alias. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- TODO: swap for the real AdMob App ID before a Product build —
             this is still Google's public test App ID. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <service
            android:name=".notification.WaterTrackingService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <service
            android:name=".notification.FCMService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".notification.ReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.NotificationClickHandler"
            android:exported="false">
            <intent-filter>
                <action android:name="com.amobi.drinkwater.ACTION_CLOSE_ONGOING" />
                <action android:name="com.amobi.drinkwater.ACTION_ADD_WATER" />
                <action android:name="com.amobi.drinkwater.ACTION_REFRESH" />
                <action android:name="com.amobi.drinkwater.ACTION_OPEN_APP" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.OngoingHealthCheckReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.AppRestartedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Home screen widgets -->
        <receiver
            android:name=".widget.SmallWidgetAProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_a_info" />
        </receiver>

        <receiver
            android:name=".widget.SmallWidgetBProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_b_info" />
        </receiver>

        <receiver
            android:name=".widget.MediumWidgetAProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_a_info" />
        </receiver>

        <receiver
            android:name=".widget.MediumWidgetBProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_b_info" />
        </receiver>

        <receiver
            android:name=".widget.LargeWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_large_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.amobi.drinkwater.WIDGET_QUICK_ADD" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.MidnightResetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.amobi.drinkwater.MIDNIGHT_RESET" />
            </intent-filter>
        </receiver>

        <service
            android:name=".widget.WidgetCountdownService"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility for Health Connect, so the app can tell whether
             it is installed and open its permission screen. -->
        <package android:name="com.google.android.apps.healthdata" />
        <!-- Needed on Android 11+ for url_launcher to hand the privacy policy
             URL to a browser; without it the launch silently fails. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
