<?xml version="1.0" encoding="utf-8"?>
<!-- Small Widget B: Amount + remaining + progress bar + Add Drink -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="160dp"
    android:layout_height="170dp"
    android:background="@drawable/widget_bg_blue_rounded"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Header: amount -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="bottom|start"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/img_water_widget"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/img_water_widget"
            android:contentDescription="Water" />

        <TextView
            android:id="@+id/tv_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="4dp"
            android:layout_marginBottom="4dp"
            android:text="1800ml"
            android:textColor="@color/clr_white"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_remaining"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginTop="2dp"
        android:text="Remaining 800ml"
        android:textColor="#D9FFFFFF"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_percentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="90%"
            android:textColor="@color/clr_white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <ProgressBar
            android:id="@+id/progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="8dp"
            android:layout_marginLeft="10dp"
            android:layout_weight="1"
            android:max="100"
            android:progress="90"
            android:progressDrawable="@drawable/widget_progress_bar_white" />
    </LinearLayout>

    <!-- Add Drink button -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp">

        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:background="@drawable/widget_btn_container_border"
            android:padding="2dp">

            <TextView
                android:id="@+id/btn_add_drink"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/widget_btn_gradient_blue"
                android:paddingLeft="24dp"
                android:paddingRight="24dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:text="@string/add_drink"
                android:textColor="@color/clr_white"
                android:textSize="12sp"
                android:fontFamily="sans-serif-medium"
                android:textStyle="normal" />
        </FrameLayout>
    </RelativeLayout>

</LinearLayout>
