import 'package:flutter/material.dart';

class ScreensReminder extends StatelessWidget {
  const ScreensReminder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Reminder Screen', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
